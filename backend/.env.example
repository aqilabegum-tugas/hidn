# HIDN Backend — environment variables
# Copy this file to backend/.env (already same values as docker-compose)
PORT=4000
DATABASE_URL=postgresql://hidn:hidn123@localhost:5432/hidn_db
JWT_SECRET=ganti-string-random-panjang-disini-buat-production
